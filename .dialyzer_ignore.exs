[
  # Phoenix.Router's generated match function has a clause for a route lookup that returns
  # :error, which a router with no routes of its own can never produce. The warning is in the
  # dependency's generated code, not in anything this app writes.
  {"deps/phoenix/lib/phoenix/router.ex", :pattern_match}
]
