pre-commit:
  parallel: true
  commands:
    check_css_formatted:
      run: mix format.css.check
    check_elixir_code_formatted:
      run: mix format --check-formatted
    check_javascript_json_and_yaml_formatted:
      run: mix format.js.check
